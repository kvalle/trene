# Issue 214 screenshots

All pairs use the same Android emulator, viewport, theme, database state, and
navigation state. The Settings pairs show light and dark mode. The Your data
pairs show both enabled and disabled delete-action states in light and dark
mode.
