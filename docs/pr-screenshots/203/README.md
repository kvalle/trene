# Issue 203 screenshots

The Data-screen pair uses the same Android emulator, viewport, light theme, and
empty database. `after-data-with-training.png` shows the enabled state after one
exercise was added. The dedicated screen and confirmation dialog do not exist
on `origin/main`, so their before state is represented by `before-data.png`.
